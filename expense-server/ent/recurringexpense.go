// Code generated by ent, DO NOT EDIT.

package ent

import (
	"expense-server/ent/category"
	"expense-server/ent/recurringexpense"
	"expense-server/ent/user"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// RecurringExpense is the model entity for the RecurringExpense schema.
type RecurringExpense struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Amount holds the value of the "amount" field.
	Amount float64 `json:"amount,omitempty"`
	// CategoryID holds the value of the "category_id" field.
	CategoryID *int `json:"category_id,omitempty"`
	// Interval holds the value of the "interval" field.
	Interval string `json:"interval,omitempty"`
	// NextRunDate holds the value of the "next_run_date" field.
	NextRunDate time.Time `json:"next_run_date,omitempty"`
	// LastRunDate holds the value of the "last_run_date" field.
	LastRunDate *time.Time `json:"last_run_date,omitempty"`
	// IsActive holds the value of the "is_active" field.
	IsActive bool `json:"is_active,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RecurringExpenseQuery when eager-loading is set.
	Edges        RecurringExpenseEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RecurringExpenseEdges holds the relations/edges for other nodes in the graph.
type RecurringExpenseEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Category holds the value of the category edge.
	Category *Category `json:"category,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RecurringExpenseEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// CategoryOrErr returns the Category value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RecurringExpenseEdges) CategoryOrErr() (*Category, error) {
	if e.Category != nil {
		return e.Category, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: category.Label}
	}
	return nil, &NotLoadedError{edge: "category"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RecurringExpense) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case recurringexpense.FieldIsActive:
			values[i] = new(sql.NullBool)
		case recurringexpense.FieldAmount:
			values[i] = new(sql.NullFloat64)
		case recurringexpense.FieldID, recurringexpense.FieldUserID, recurringexpense.FieldCategoryID:
			values[i] = new(sql.NullInt64)
		case recurringexpense.FieldTitle, recurringexpense.FieldInterval:
			values[i] = new(sql.NullString)
		case recurringexpense.FieldNextRunDate, recurringexpense.FieldLastRunDate, recurringexpense.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RecurringExpense fields.
func (re *RecurringExpense) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case recurringexpense.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			re.ID = int(value.Int64)
		case recurringexpense.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				re.UserID = int(value.Int64)
			}
		case recurringexpense.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				re.Title = value.String
			}
		case recurringexpense.FieldAmount:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field amount", values[i])
			} else if value.Valid {
				re.Amount = value.Float64
			}
		case recurringexpense.FieldCategoryID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field category_id", values[i])
			} else if value.Valid {
				re.CategoryID = new(int)
				*re.CategoryID = int(value.Int64)
			}
		case recurringexpense.FieldInterval:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field interval", values[i])
			} else if value.Valid {
				re.Interval = value.String
			}
		case recurringexpense.FieldNextRunDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field next_run_date", values[i])
			} else if value.Valid {
				re.NextRunDate = value.Time
			}
		case recurringexpense.FieldLastRunDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_run_date", values[i])
			} else if value.Valid {
				re.LastRunDate = new(time.Time)
				*re.LastRunDate = value.Time
			}
		case recurringexpense.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				re.IsActive = value.Bool
			}
		case recurringexpense.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				re.CreatedAt = value.Time
			}
		default:
			re.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RecurringExpense.
// This includes values selected through modifiers, order, etc.
func (re *RecurringExpense) Value(name string) (ent.Value, error) {
	return re.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the RecurringExpense entity.
func (re *RecurringExpense) QueryUser() *UserQuery {
	return NewRecurringExpenseClient(re.config).QueryUser(re)
}

// QueryCategory queries the "category" edge of the RecurringExpense entity.
func (re *RecurringExpense) QueryCategory() *CategoryQuery {
	return NewRecurringExpenseClient(re.config).QueryCategory(re)
}

// Update returns a builder for updating this RecurringExpense.
// Note that you need to call RecurringExpense.Unwrap() before calling this method if this RecurringExpense
// was returned from a transaction, and the transaction was committed or rolled back.
func (re *RecurringExpense) Update() *RecurringExpenseUpdateOne {
	return NewRecurringExpenseClient(re.config).UpdateOne(re)
}

// Unwrap unwraps the RecurringExpense entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (re *RecurringExpense) Unwrap() *RecurringExpense {
	_tx, ok := re.config.driver.(*txDriver)
	if !ok {
		panic("ent: RecurringExpense is not a transactional entity")
	}
	re.config.driver = _tx.drv
	return re
}

// String implements the fmt.Stringer.
func (re *RecurringExpense) String() string {
	var builder strings.Builder
	builder.WriteString("RecurringExpense(")
	builder.WriteString(fmt.Sprintf("id=%v, ", re.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", re.UserID))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(re.Title)
	builder.WriteString(", ")
	builder.WriteString("amount=")
	builder.WriteString(fmt.Sprintf("%v", re.Amount))
	builder.WriteString(", ")
	if v := re.CategoryID; v != nil {
		builder.WriteString("category_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("interval=")
	builder.WriteString(re.Interval)
	builder.WriteString(", ")
	builder.WriteString("next_run_date=")
	builder.WriteString(re.NextRunDate.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := re.LastRunDate; v != nil {
		builder.WriteString("last_run_date=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", re.IsActive))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(re.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RecurringExpenses is a parsable slice of RecurringExpense.
type RecurringExpenses []*RecurringExpense
